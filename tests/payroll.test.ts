import {describe,it,expect} from 'vitest';import {calculateDay,roundMoney} from '@/lib/payroll/calculate-day';import {calculateOvertime} from '@/lib/payroll/overtime';import {calculatePeriod} from '@/lib/payroll/calculate-period';
describe('payroll calculation',()=>{it('calculates a full and partial workday',()=>{expect(calculateDay({workedMinutes:480,dailyRate:800,standardDailyHours:8}).regularPay).toBe(800);expect(calculateDay({workedMinutes:240,dailyRate:800,standardDailyHours:8}).regularPay).toBe(400)});it('rounds PHP values consistently',()=>expect(roundMoney(1.005)).toBe(1.01));it('excludes rejected overtime',()=>expect(calculateOvertime(800,8,60,1.25,'rejected')).toBe(0));it('calculates approved overtime',()=>expect(calculateOvertime(800,8,120,1.25,'approved')).toBe(250));it('handles adjustments, incomplete logs and overrides',()=>{const result=calculatePeriod([{workedMinutes:480,dailyRate:800,complete:true,overtimeMinutes:60,overtimeStatus:'approved'},{workedMinutes:480,dailyRate:900,complete:false}],8,[{type:'bonus',amount:100},{type:'positive_adjustment',amount:50},{type:'negative_adjustment',amount:25}],1000);expect(result.computedGrossPay).toBe(1050);expect(result.finalPayoutAmount).toBe(1000)})});
