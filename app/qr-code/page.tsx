import {requireEmployee} from '@/lib/auth/sessions';
export default async function QrCode(){await requireEmployee();return <section className="mx-auto max-w-xl"><h1 className="text-3xl font-bold">Your QR code</h1><div className="card mt-5"><p>Upload is served by a protected server route in the production Supabase deployment. Only PNG, JPEG, and WebP up to 5 MB are accepted; storage paths are random and owned by your signed employee session.</p><label className="mt-4 block font-medium">QR image<input className="mt-1 block w-full" type="file" accept="image/png,image/jpeg,image/webp"/></label><button className="mt-4" disabled>Configure Supabase Storage to upload</button></div></section>}
