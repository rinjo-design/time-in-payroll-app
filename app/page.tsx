import {selectEmployee} from './actions'; import {supabaseAdmin} from '@/lib/supabase/server';
export default async function Home(){let employees:{id:string;display_name:string;employee_code:string}[]=[];try{const r=await supabaseAdmin().from('employees').select('id,display_name,employee_code').eq('is_active',true).order('display_name');employees=r.data??[]}catch{}return <section className="mx-auto max-w-xl space-y-6"><div><p className="text-sm font-semibold text-indigo-700">WORKPLACE TIME CLOCK</p><h1 className="text-3xl font-bold">Select your name</h1><p className="mt-2 text-slate-600">This kiosk identifies you for attendance only. My Pay always needs your private PIN.</p></div><form action={selectEmployee} className="card space-y-4"><label className="block font-medium" htmlFor="employeeId">Active employee</label><select id="employeeId" name="employeeId" required className="w-full"><option value="">Choose your name</option>{employees.map(e=><option key={e.id} value={e.id}>{e.display_name} ({e.employee_code})</option>)}</select><button className="w-full min-h-14" type="submit">Continue to time clock</button>{!employees.length&&<p className="text-sm text-slate-500">No active employees are available. An administrator can add one.</p>}</form></section>}
