import {requireAdmin} from '@/lib/auth/sessions'; export default async function Page(){await requireAdmin();return <section><h1 className="text-3xl font-bold">Admin settings</h1><div className="card mt-5"><p>This protected administrator workspace is ready for Supabase-backed management operations. All actions must be validated server-side and recorded in <code>admin_audit_logs</code>.</p></div></section>}
