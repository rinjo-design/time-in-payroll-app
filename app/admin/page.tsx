import {getSession,setSession} from '@/lib/auth/sessions';import bcrypt from 'bcryptjs';import {redirect} from 'next/navigation';
async function login(formData:FormData){'use server';const password=String(formData.get('password')??'');const hash=process.env.ADMIN_PASSWORD_HASH;if(!hash||!(await bcrypt.compare(password,hash)))throw new Error('Unable to sign in.');await setSession('admin','administrator',60);redirect('/admin/dashboard');}
export default async function Admin(){if(await getSession('admin'))redirect('/admin/dashboard');return <section className="mx-auto max-w-md"><h1 className="text-3xl font-bold">Administrator sign in</h1><form action={login} className="card mt-5 space-y-4"><label className="block">Password<input className="mt-1 w-full" name="password" type="password" required/></label><button className="w-full">Sign in</button><p className="text-sm text-slate-600">Password verification happens only on the server.</p></form></section>}
